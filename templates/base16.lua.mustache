-- Auto-generated by tinted-builder-rust from a base16 scheme.
-- Translates base16 slots into tinted8-shaped keys. Per-key justification
-- lives in the per-highlight audit; in short, every key fill below is
-- driven by current-code rendering of base16 themes (parity-preserving).
-- Do not edit by hand.

return {
    variant = "{{ scheme-variant }}",

    -- Legacy base16 slot keys (kept for backwards compatibility).
    -- New consumers should read the tinted8-shaped keys below.
    base00 = "#{{ base00-hex }}",
    base01 = "#{{ base01-hex }}",
    base02 = "#{{ base02-hex }}",
    base03 = "#{{ base03-hex }}",
    base04 = "#{{ base04-hex }}",
    base05 = "#{{ base05-hex }}",
    base06 = "#{{ base06-hex }}",
    base07 = "#{{ base07-hex }}",
    base08 = "#{{ base08-hex }}",
    base09 = "#{{ base09-hex }}",
    base0A = "#{{ base0A-hex }}",
    base0B = "#{{ base0B-hex }}",
    base0C = "#{{ base0C-hex }}",
    base0D = "#{{ base0D-hex }}",
    base0E = "#{{ base0E-hex }}",
    base0F = "#{{ base0F-hex }}",

    -- Named palette colors. base16 has no bright/dim variants,
    -- so .bright falls back to .normal (matches legacy alias behavior).
    palette = {
        black = {
            normal = "#{{ base00-hex }}",
            bright = "#{{ base01-hex }}",
        },
        gray = {
            dim = "#{{ base02-hex }}",
            normal = "#{{ base03-hex }}",
            -- base06 (bright_white) is unused by tinted-nvim highlights; left unmapped.
        },
        white = {
            dim = "#{{ base04-hex }}",
            normal = "#{{ base05-hex }}",
            bright = "#{{ base07-hex }}",
        },
        red     = { normal = "#{{ base08-hex }}", bright = "#{{ base08-hex }}" },
        orange  = { normal = "#{{ base09-hex }}", bright = "#{{ base09-hex }}" },
        yellow  = { normal = "#{{ base0A-hex }}", bright = "#{{ base0A-hex }}" },
        green   = { normal = "#{{ base0B-hex }}", bright = "#{{ base0B-hex }}" },
        cyan    = { normal = "#{{ base0C-hex }}", bright = "#{{ base0C-hex }}" },
        blue    = { normal = "#{{ base0D-hex }}", bright = "#{{ base0D-hex }}" },
        magenta = { normal = "#{{ base0E-hex }}", bright = "#{{ base0E-hex }}" },
        brown   = { normal = "#{{ base0F-hex }}" },
    },

    -- UI keys, filled from base16 slots to preserve current rendering.
    ui = {
        global = {
            background = { normal = "#{{ base00-hex }}" },
            foreground = { normal = "#{{ base05-hex }}" },
        },
        cursor = {
            background = { normal = "#{{ base05-hex }}" },
            foreground = { normal = "#{{ base00-hex }}" },
        },
        gutter = { foreground = "#{{ base02-hex }}" },
        border = { normal = "#{{ base03-hex }}" },
        chrome = {
            background = {
                normal = "#{{ base02-hex }}",
                dark = "#{{ base01-hex }}",
            },
            foreground = {
                normal = "#{{ base05-hex }}",
                dark = "#{{ base04-hex }}",
            },
        },
        selection = { background = "#{{ base01-hex }}" },
        highlight = {
            line = { background = "#{{ base01-hex }}" },
            text = { background = "#{{ base02-hex }}" },
            search = {
                background = "#{{ base01-hex }}",
                foreground = "#{{ base0A-hex }}",
            },
        },
        status = {
            error = "#{{ base08-hex }}",
            warning = "#{{ base09-hex }}",
            info = "#{{ base0A-hex }}",
            success = "#{{ base0B-hex }}",
        },
    },

    -- Syntax keys.
    syntax = {
        comment = "#{{ base03-hex }}",

        string = {
            default = "#{{ base0B-hex }}",
            regexp = "#{{ base0B-hex }}",
            other = "#{{ base0B-hex }}",
        },

        constant = {
            default = "#{{ base09-hex }}",
            character = {
                default = "#{{ base08-hex }}",
                escape = "#{{ base0C-hex }}",
            },
            language = "#{{ base09-hex }}",
            numeric = {
                default = "#{{ base09-hex }}",
                float = "#{{ base09-hex }}",
            },
        },

        entity = {
            name = {
                class = "#{{ base0A-hex }}",
                type = "#{{ base0A-hex }}",
                ["function"] = {
                    default = "#{{ base0D-hex }}",
                    constructor = "#{{ base0D-hex }}",
                },
                label = "#{{ base0A-hex }}",
                namespace = "#{{ base08-hex }}",
                tag = "#{{ base0A-hex }}",
            },
            other = {
                ["attribute-name"] = "#{{ base0A-hex }}",
            },
        },

        keyword = {
            default = "#{{ base0E-hex }}",
            control = {
                default = "#{{ base0E-hex }}",
                import = "#{{ base0D-hex }}",
                flow = "#{{ base0A-hex }}",
            },
            operator = "#{{ base0E-hex }}",
            declaration = "#{{ base0E-hex }}",
        },

        storage = {
            type = "#{{ base0E-hex }}",
            modifier = "#{{ base0A-hex }}",
        },

        variable = {
            default = "#{{ base05-hex }}",
            parameter = "#{{ base05-hex }}",
            other = {
                property = "#{{ base05-hex }}",
            },
        },

        punctuation = {
            separator = "#{{ base0F-hex }}",
            section = "#{{ base0F-hex }}",
        },

        markup = {
            default = "#{{ base05-hex }}",
            heading = "#{{ base0D-hex }}",
            raw = "#{{ base09-hex }}",
            link = "#{{ base09-hex }}",
            list = "#{{ base0A-hex }}",
            inserted = "#{{ base0B-hex }}",
            deleted = "#{{ base08-hex }}",
        },

        meta = {
            preprocessor = "#{{ base0A-hex }}",
        },
    },
}
