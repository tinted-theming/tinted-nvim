-- Auto-generated by tinted-builder-rust from a tinted8 scheme.
-- Do not edit by hand.

return {
    variant = "{{variant}}",

    -- Named palette colors. Variants beyond what the scheme defines
    -- are generated by the tinted8 builder per its spec.
    palette = {
        black = {
            normal = "#{{palette.black.normal.hex}}",
            bright = "#{{palette.black.bright.hex}}",
        },
        gray = {
            dim = "#{{palette.gray.dim.hex}}",
            normal = "#{{palette.gray.normal.hex}}",
            bright = "#{{palette.gray.bright.hex}}",
        },
        white = {
            dim = "#{{palette.white.dim.hex}}",
            normal = "#{{palette.white.normal.hex}}",
            bright = "#{{palette.white.bright.hex}}",
        },
        red = {
            normal = "#{{palette.red.normal.hex}}",
            bright = "#{{palette.red.bright.hex}}",
        },
        orange = {
            normal = "#{{palette.orange.normal.hex}}",
            bright = "#{{palette.orange.bright.hex}}",
        },
        yellow = {
            normal = "#{{palette.yellow.normal.hex}}",
            bright = "#{{palette.yellow.bright.hex}}",
        },
        green = {
            normal = "#{{palette.green.normal.hex}}",
            bright = "#{{palette.green.bright.hex}}",
        },
        cyan = {
            normal = "#{{palette.cyan.normal.hex}}",
            bright = "#{{palette.cyan.bright.hex}}",
        },
        blue = {
            normal = "#{{palette.blue.normal.hex}}",
            bright = "#{{palette.blue.bright.hex}}",
        },
        magenta = {
            normal = "#{{palette.magenta.normal.hex}}",
            bright = "#{{palette.magenta.bright.hex}}",
        },
        brown = { normal = "#{{palette.brown.normal.hex}}" },
    },

    -- UI keys (tinted8 spec).
    ui = {
        global = {
            background = { normal = "#{{ui.global.background.normal.hex}}" },
            foreground = { normal = "#{{ui.global.foreground.normal.hex}}" },
        },
        cursor = {
            background = { normal = "#{{ui.cursor.background.normal.hex}}" },
            foreground = { normal = "#{{ui.cursor.foreground.normal.hex}}" },
        },
        gutter = { foreground = "#{{ui.gutter.foreground.hex}}" },
        border = { normal = "#{{ui.border.normal.hex}}" },
        chrome = {
            background = {
                normal = "#{{ui.chrome.background.normal.hex}}",
                dark = "#{{ui.chrome.background.dark.hex}}",
            },
            foreground = {
                normal = "#{{ui.chrome.foreground.normal.hex}}",
                dark = "#{{ui.chrome.foreground.dark.hex}}",
            },
        },
        selection = { background = "#{{ui.selection.background.hex}}" },
        highlight = {
            line = { background = "#{{ui.highlight.line.background.hex}}" },
            text = { background = "#{{ui.highlight.text.background.hex}}" },
            search = {
                background = "#{{ui.highlight.search.background.hex}}",
                foreground = "#{{ui.highlight.search.foreground.hex}}",
            },
        },
        status = {
            error = "#{{ui.status.error.hex}}",
            warning = "#{{ui.status.warning.hex}}",
            info = "#{{ui.status.info.hex}}",
            success = "#{{ui.status.success.hex}}",
        },
    },

    -- Syntax keys (tinted8 spec).
    -- Parent-with-children nodes use `default` for the parent's own color.
    syntax = {
        comment = "#{{syntax.comment.default.hex}}",

        string = {
            default = "#{{syntax.string.default.hex}}",
            regexp = "#{{syntax.string.regexp.hex}}",
            other = "#{{syntax.string.other.hex}}",
        },

        constant = {
            default = "#{{syntax.constant.default.hex}}",
            character = {
                default = "#{{syntax.constant.character.default.hex}}",
                escape = "#{{syntax.constant.character.escape.hex}}",
            },
            language = "#{{syntax.constant.language.hex}}",
            numeric = {
                default = "#{{syntax.constant.numeric.default.hex}}",
                float = "#{{syntax.constant.numeric.float.hex}}",
            },
        },

        entity = {
            name = {
                class = "#{{syntax.entity.name.class.hex}}",
                type = "#{{syntax.entity.name.type.default.hex}}",
                ["function"] = {
                    default = "#{{syntax.entity.name.function.default.hex}}",
                    constructor = "#{{syntax.entity.name.function.constructor.hex}}",
                },
                label = "#{{syntax.entity.name.label.hex}}",
                namespace = "#{{syntax.entity.name.namespace.hex}}",
                tag = "#{{syntax.entity.name.tag.hex}}",
            },
            other = {
                ["attribute-name"] = "#{{syntax.entity.other.attribute-name.hex}}",
            },
        },

        keyword = {
            default = "#{{syntax.keyword.default.hex}}",
            control = {
                default = "#{{syntax.keyword.control.default.hex}}",
                import = "#{{syntax.keyword.control.import.hex}}",
                flow = "#{{syntax.keyword.control.flow.hex}}",
            },
            operator = "#{{syntax.keyword.operator.hex}}",
            declaration = "#{{syntax.keyword.declaration.hex}}",
        },

        storage = {
            type = "#{{syntax.storage.type.hex}}",
            modifier = "#{{syntax.storage.modifier.hex}}",
        },

        variable = {
            default = "#{{syntax.variable.default.hex}}",
            parameter = "#{{syntax.variable.parameter.hex}}",
            other = {
                property = "#{{syntax.variable.other.property.hex}}",
            },
        },

        punctuation = {
            separator = "#{{syntax.punctuation.separator.hex}}",
            section = "#{{syntax.punctuation.section.hex}}",
        },

        markup = {
            default = "#{{syntax.markup.default.hex}}",
            heading = "#{{syntax.markup.heading.hex}}",
            raw = "#{{syntax.markup.raw.hex}}",
            link = "#{{syntax.markup.link.hex}}",
            list = "#{{syntax.markup.list.default.hex}}",
            inserted = "#{{syntax.markup.inserted.hex}}",
            deleted = "#{{syntax.markup.deleted.hex}}",
        },

        meta = {
            preprocessor = "#{{syntax.meta.preprocessor.hex}}",
        },
    },
}
